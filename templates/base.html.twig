<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="author" content="Paul Brousses"/>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/fork-awesome@1.1.7/css/fork-awesome.min.css" integrity="sha256-gsmEoJAws/Kd3CjuOQzLie5Q3yshhvmo7YNtBG7aaEY=" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
        <link rel="stylesheet" href="{{ asset('assets/css/styles.css') }}">
        <link href="https://fonts.googleapis.com/css2?family=Galada&family=Roboto&display=swap" rel="stylesheet">
        <title>{% block title %}MGL - Courtier en Assurance Professionnels et Particuliers{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block nav %}
            <header class="sticky-top">
            <nav class="navbar justify-content-space-around navbar-top">
                <a class="navbar-brand" href="{{ path('home') }}">
                    <img src="{{ asset('assets/images/logo.jpg') }}" height="80px" alt="logo" loading="lazy" class="d-none d-sm-block">
                    <img src="{{ asset('assets/images/logo.jpg') }}" height="60px" alt="logo" loading="lazy" class="d-sm-none">
                </a>
                <div class="slogan d-none d-sm-block">
                    Votre assureur depuis 1983
                </div>
                <div class="infoNav d-none d-sm-block">
                    <i class="fa fa-phone fa-2x" aria-hidden="true"> 04 67 74 24 74</i>
                    <p> Ouvert du lundi au vendredi de 9h à 18h </p>
                    <i class="fa fa-facebook fa-2x" alt="lien facebook" aria-hidden="true"></i>
                    <i class="fa fa-twitter fa-2x" alt="lien twitter" aria-hidden="true"></i>
            </nav>
        {% set route = app.request.attributes.get('_route') %}
        {% set appSubCategory = app.request.attributes.get('SousCategorie') %}
        {% set appType = app.request.attributes.get('type') %}

            <nav id="menu">
                <label for="tm" id="toggle-menu"><span class="drop-icon-burger">&equiv;</span></label>
                <input type="checkbox" id="tm">

                    <ul class="main-menu clearfix">
                    <li>
                    {% if route == 'home' %}
                    <a class="nav-link active" href="{{ path('home') }}">Accueil<span class="sr-only">(current)</span></a></li>
                    {% else %}
                    <a class="nav-link" href="{{ path('home') }}">Accueil<span class="sr-only">(current)</span></a></li>
                    {% endif %}
                    </li>

                    {% for category in allCategory %}
                    <li>
                        {% if category.slug == app.request.attributes.get('slug') %}
                        <a class="nav-link active" href="{{ path('assurances_categorie', {'slug': category.slug}) }}">{{category.name}} <span class="drop-icon">▾</span><label title="Toggle Drop-down" class="drop-icon" for="sm{{loop.index}}"><span class="fa fa-plus fa-lg" aria-hidden="true"></span></label></a>

                        {% elseif appSubCategory is not null and appSubCategory.category == category %}
                        <a class="nav-link active" href="{{ path('assurances_categorie', {'slug': category.slug}) }}">{{category.name}} <span class="drop-icon">▾</span><label title="Toggle Drop-down" class="drop-icon" for="sm{{loop.index}}"><span class="fa fa-plus fa-lg" aria-hidden="true"></span></label></a>

                        {% elseif appType is not null and appType.subCategory.category == category %}
                        <a class="nav-link active" href="{{ path('assurances_categorie', {'slug': category.slug}) }}">{{category.name}} <span class="drop-icon">▾</span><label title="Toggle Drop-down" class="drop-icon" for="sm{{loop.index}}"><span class="fa fa-plus fa-lg" aria-hidden="true"></span></label></a>

                        {% else %}
                        <a class="nav-link" href="{{ path('assurances_categorie', {'slug': category.slug}) }}">{{category.name}} <span class="drop-icon">▾</span><label title="Toggle Drop-down" class="drop-icon" for="sm{{loop.index}}"><span class="fa fa-plus fa-lg" aria-hidden="true"></span></label></a>
                        {% endif %}

                        <input type="checkbox" id="sm{{loop.index}}">
                        <ul class="sub-menu">
                        {% for sousCategories in category.SousCategories %}
                        <li><a class="nav-link" href="{{ path('assurances_sous-categorie', {'slug': sousCategories.slug}) }}">{{sousCategories.name}}
                            {% if category.id == 1 %}
                            <label title="Toggle Drop-down" class="drop-icon" for="sm{{100 + loop.index}}"></label>
                            {% else %}
                            <label title="Toggle Drop-down" class="drop-icon" for="sm{{200 + loop.index}}"></label>
                            {% endif %}
                            </a>
                            {% if category.id == 1 %}
                            <input type="checkbox" id="sm{{100 + loop.index}}">
                            {% else %}
                            <input type="checkbox" id="sm{{200 + loop.index}}">
                            {% endif %}
                        </li>
                        {% endfor %}
                        </ul>
                        
                    </li>
                    {% endfor %}
                    <li>
                        <form class="form-inline my-2 align-self-center d-lg-flex flex-nowrap" method="get">
                        <input class="form-control mr-sm-2" name="search" type="text" placeholder="Rechercher" aria-label="Rechercher">
                        <button class="btn my-sm-0 btn-search" type="submit" formaction=""><i class="fa fa-search" aria-hidden="true"></i></button>
                        </form>
                    </li>
                    <li>
                    <a class="nav-link" href="{{ path('app_register')}}">Inscription</a>
                    </li>
                    <li>
                    <a class="nav-link" href="" data-toggle="modal" data-target="#connexionModale">Connexion</a>
                    </li>
                    </ul>
                </nav>


        </header>

        {% endblock %}
        {{ include('partials/modal-login.html.twig') }}

        <div class="container-nav-body">
            {% block body %}{% endblock %}  
        </div>   
        {% block footer %}
        
        <footer>
            <div>
                <a class="footer-link" href="#">Plan du site</a> - 
                <a class="footer-link" href="#">Mentions légales</a> - 
                <a class="footer-link" href="#">Contact</a> - 
                <a class="footer-link" href="#">A propos</a>
            </div>
                <p class="mt-4">Tous droits réservés © 2020 MGL Courtage</p>
        </footer>
        {% endblock %}
            {% block javascripts %}    
            <script src="https://code.jquery.com/jquery-3.5.1.min.js" integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=" crossorigin="anonymous"></script>
            <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
            <script src="{{ asset('assets/js/app.js') }}"></script>
            <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>
        {% endblock %}
    </body>
</html>
   